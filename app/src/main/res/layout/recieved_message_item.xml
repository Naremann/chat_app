<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="receiver_message"
            type="com.example.chatapplication.model.Message" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="30dp"
        android:paddingVertical="10dp"
        >
        <TextView
            android:id="@+id/sender_name_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@{receiver_message.senderName}"
            android:textColor="@color/black"
            tools:text="Ahmed"
            android:textSize="25sp" />

        <EditText
            android:id="@+id/sent_message_et"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/sender_name_tv"
            android:layout_marginTop="5dp"
            android:text="@{receiver_message.messageContent}"
            android:background="@drawable/send_message_et"
            android:padding="10dp"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/message_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/sent_message_et"
            app:layout_constraintStart_toEndOf="@id/sent_message_et"
            android:text="@{receiver_message.time}"
            style="@style/MessageTimeStyle"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>